namespace RealServ.Shared.Domain.Enums;

public enum UserType
{
    Buyer = 1,
    Vendor = 2,
    Admin = 3
}

public enum UserStatus
{
    Active = 1,
    Inactive = 2,
    Suspended = 3,
    Deleted = 4
}
